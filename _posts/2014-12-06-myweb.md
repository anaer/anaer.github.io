---
layout: post
title: "myweb修改记录"
description: "myweb修改记录"
category:myweb
tags: [myweb]

---

#### DEAD 添加本地线程变量
    --每次系统启动时，尝试连接外网，判断网络是否正常，记录网络状态到ThreadLocal中，定时任务中先判断网络，再确定是否执行下去

#### DEAD 更新规则处理
    --找默认规则
    getDomainFromSite(Book.url)
    getCssQueryFromDomain(String domain)
    
    --获取顶级域名
    getTopDomain()

    模板管理
    接口模板
    注释
    建表

#### DONE 添加提醒表
    notify
    status(0:未提醒, 1:已提醒)
    
    DROP TABLE IF EXISTS `notify`;
    CREATE TABLE `notify` (
      `id` int(11) NOT NULL,
      `type` varchar(255) DEFAULT NULL,
      `message` varchar(255) DEFAULT NULL,
      `status` varchar(255) DEFAULT NULL,
      `notify_time` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
      PRIMARY KEY (`id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
#### DONE 开启浏览器桌面提醒
    添加表notify
    页面添加checkbox 自动获取 提醒
#### DEAD 添加更新策略表
    updaterule
    字段
    regex 正则匹配链接
    rule 规则
    需要查询下如何根据url去数据库匹配正则数据
#### DEAD dict分为主表与子项表
    暂时没必要修改
#### DEAD 处理规则
    rules
      是否可用:是/否
      规则类型
      处理类型:url/domain
      匹配方式:normal/regex
      匹配串:
      处理方式:忽略/保留最新
#### DEAD 操作日志  暂时不需要
      流水号
      时间
      日志级别: debug/info/error/warning
      日志来源: 自动/手动
      日志信息
#### DONE 添加快捷查询 保存当前查询 存储为json串

    界面上显示为快捷下拉查询，
    下拉框显示 id-name
    选择快捷查询后，后台从数据库获取到选中的快捷查询信息，将json串 转换为查询条件对象 从而到数据库进行查询

    如果需要将该功能作为公共功能, 表中添加class字段，自动查询 并使用反射生成查询条件对象

    快捷查询表
    id  name  json type class
    
    * 2015-05-05
    复用数据字典查询功能, 添加快速查询后，在数据字典中添加相应的值
    如果这样, 那么在fast_query表中就不需要记录category及clazz字段了
    可以根据数据字典 找到对应的信息
    
    如果直接将快速查询的json保存到dict中, 就没必要添加表fast_query
    
    对快捷查询的维护 暂时还是到数据字典界面进行维护
#### TODO problem详情 界面布局
    参考csdn blog 界面布局
#### DONE 导出时，导出参数中 sheetName为空 则不生成 标题行
#### DONE 对于单表查询，多表关联查询, 且查询的字段全是主表的字段 的处理
    1. 在sqlmap中，将所有查询分类处理，独立成一个个的select
    1.1 这样如果查询多的话，sqlmap内容会比较多
    1.2 相对应的mapper也不少
    2. 使用视图，将多表关联查询建成视图
    2.1 因为字段多一样，所以可以共用一个select
    2.1.1 视图名 使用原表+后缀的形式, 在sqlmap中使用tblname${suffix}方式, 后缀动态参数, 如果后缀没传，可以默认使用原表查询，不会导致sql出错
    2.1.1.1 一个问题,suffix在代码中配置, 如果改视图名，就得改代码
    2.1.1.2 suffix在表中配置，查询时根据查询条件中的某个特定字段，如viewid或queryType等，这个可以使用数据字典，前台下拉框显示
    2.1.1.2.1 添加视图配置表viewmanage( viewid, viewname, viewdesc, suffix)
                                            根据viewid 到viewmanage查询对应的后缀名suffix
    2.1.1.2.1.1 存在问题,每次查询都得先去数据库查询一次后缀,影响效率
    2.1.1.2.1.2 使用memcache缓存首次读取的表名, 从缓存取? `这个还没试过.`
    2.1.1.2.2 通过这个特定字段的值，找到视图名? 比如这个字段的值就是视图名，或者可以原表名组合生成视图名?
    2.1.1.2.2.1 命名规则如： 原表名代码中配置的固定字段前台返回的特定字段值  `可以试下`
                sqlmap配置 tblname$(suffix)  suffix=代码中配置的固定字段前台返回的特定字段值
    2.1.1.2.2.1.1 还有个问题，如果这种关联查询很多，就会有很多视图
    3. 合并同类的关联查询，调整表结构，使它们能共用一个select
    4. domain添加类型字段  关联site查询， 查询条件加类型字段
    5. 直接在site表添加状态字段了
#### DONE 菜单 管理
#### DONE dict数据字典 支持包含空项
#### DONE dict字典翻译 保存 格式化，支持1维，2维数组
#### DONE site如name为空,则默认不可见
#### DONE site删除排序，权重字段，添加显示/隐藏标志 visiable
#### DONE book添加更新方式 update_type 0: 自动 1:手动
#### DONE site添加更新方式 update_type 0:自动 1:手动
#### DONE 域表
      序号
      域名
      处理方式
#### DEAD domain添加类型字段：如论坛 博客 小说 等
    查询site时 关联，该字段作为查询条件
#### DEAD 调整为restful风格url
    先不调整了, 调整的话jsp调整有点多